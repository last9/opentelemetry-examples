# Last9 OpenTelemetry Configuration - Example for Angular
# This file is for documentation purposes only.
#
# For Angular applications, configure these values in:
# - src/environment.ts (for development)
# - src/environment.prod.ts (for production)
#
# Or inject them at build time using environment variables and Angular's
# environment replacement feature.

# IMPORTANT SECURITY NOTE:
# For browser applications, use ORIGIN-RESTRICTED CLIENT TOKENS, NOT backend authorization tokens!
#
# Steps to configure:
# 1. Get your endpoint URL from Last9 dashboard: <your-base-url>/v1/otlp/organizations/<your-org-slug>/telemetry/client_monitoring/v1/traces
# 2. Create a client token at: https://app.last9.io/control-plane/ingestion-tokens
# 3. Select "Client Token" type (not backend authorization token)
# 4. Set allowed origins (e.g., http://localhost:4200 for dev, https://yourdomain.com for prod)
# 5. Use separate tokens for development and production environments
#
# Additional Resources:
# - Getting Started: https://last9.io/docs/rum-getting-started/
# - See README.md for detailed setup instructions

# Service name (appears in Last9 dashboard)
# Update in: src/environment.ts -> otel.serviceName
OTEL_SERVICE_NAME=angular-demo-app

# Last9 OTLP endpoint for your organization
# Format: <your-base-url>/v1/otlp/organizations/<your-org-slug>/telemetry/client_monitoring/v1/traces
# Update in: src/environment.ts -> otel.endpoint
OTEL_ENDPOINT=<your-base-url>/v1/otlp/organizations/<your-org-slug>/telemetry/client_monitoring/v1/traces

# Your Client Token (from Ingestion Tokens page)
# Update in: src/environment.ts -> otel.apiToken
OTEL_API_TOKEN=your-client-token-here

# Allowed origin (must match token configuration)
# Update in: src/environment.ts -> otel.origin
OTEL_ORIGIN=http://localhost:4200

# Environment identifier
# Update in: src/environment.ts -> otel.environment
OTEL_ENVIRONMENT=development

# For production, update src/environment.prod.ts with:
# - Production endpoint URL (same format as above)
# - Production client token (separate from dev token!)
# - Production origin (https://yourdomain.com)
# - environment: 'production'
