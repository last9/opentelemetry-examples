apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: express-otel-demo
  labels:
    cloud.googleapis.com/location: us-central1
spec:
  template:
    metadata:
      annotations:
        autoscaling.knative.dev/minScale: "0"
        autoscaling.knative.dev/maxScale: "10"
        autoscaling.knative.dev/maxConcurrentRequests: "80"
    spec:
      timeoutSeconds: 300
      containerConcurrency: 80
      containers:
        - image: gcr.io/PROJECT_ID/express-otel-demo:latest
          ports:
            - containerPort: 8080
          resources:
            limits:
              cpu: "1"
              memory: "512Mi"
          env:
            - name: OTEL_SERVICE_NAME
              value: "express-cloud-run-demo"
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "https://otlp.last9.io"
            - name: OTEL_EXPORTER_OTLP_HEADERS
              valueFrom:
                secretKeyRef:
                  key: latest
                  name: last9-auth-header
            - name: OTEL_TRACES_SAMPLER
              value: "always_on"
            - name: DEPLOYMENT_ENVIRONMENT
              value: "production"
            - name: SERVICE_VERSION
              value: "1.0.0"
            - name: NODE_ENV
              value: "production"
            - name: GOOGLE_CLOUD_PROJECT
              value: "PROJECT_ID"
            - name: CLOUD_RUN_REGION
              value: "us-central1"
          startupProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 0
            periodSeconds: 2
            failureThreshold: 30
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            periodSeconds: 10
