# OpenTelemetry Configuration
OTEL_SERVICE_NAME=cloud-run-functions-demo
OTEL_EXPORTER_OTLP_ENDPOINT=<your-otlp-endpoint>
OTEL_EXPORTER_OTLP_HEADERS=Authorization=Basic <your-base64-credentials>

# Optional: Service metadata
SERVICE_VERSION=1.0.0
DEPLOYMENT_ENVIRONMENT=development

# Local development: specify which function to run
FUNCTION_TARGET=helloHttp

# Optional: URL of the service to chain calls to (for /chain endpoint)
SERVICE_URL=https://your-service-url.run.app

# Optional: URL of another function to chain calls to (for /call-function endpoint)
FUNCTION_URL=https://your-other-function-url.run.app
