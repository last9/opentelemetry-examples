# OpenResty service identity
OTEL_SERVICE_NAME=openresty
OTEL_SERVICE_VERSION=1.0.0
DEPLOYMENT_ENVIRONMENT=production

# Sampling strategy (default: parentbased_always_on)
# Options: always_on | always_off | traceid_ratio
#          parentbased_always_on | parentbased_traceid_ratio | parentbased_always_off
OTEL_TRACES_SAMPLER=parentbased_always_on
# Ratio for traceid_ratio / parentbased_traceid_ratio (0.0–1.0). Default: 1.0
# OTEL_TRACES_SAMPLER_ARG=0.1

# Maximum length for string span attributes before truncation. Default: 256
# OTEL_ATTRIBUTE_VALUE_LENGTH_LIMIT=256

# Last9 credentials — fill in from your Last9 dashboard
# The OTel Collector config uses these at startup
LAST9_OTLP_ENDPOINT=<your-last9-otlp-endpoint>
LAST9_OTLP_AUTH=<your-last9-otlp-auth-header>
